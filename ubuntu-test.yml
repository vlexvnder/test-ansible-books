---
  - name: "Playing with Ansible and Git"
    hosts: localhost
    connection: local 
    tasks:

    - name: "copy a blank file"
        copy:
          content: ""
          dest: /home/ansible_proof
          force: false
          group: sys
          owner: root
          mode: 0555
